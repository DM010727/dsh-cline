{
  "name": "dsh-cline-root",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@10.28.2",
  "engines": {
    "node": "^22.19.0 || >=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck": "pnpm -r --if-present run typecheck",
    "watch": "pnpm --filter dsh-cline run watch"
  },
  "devDependencies": {
    "@vscode/vsce": "^3.9.2"
  }
}
